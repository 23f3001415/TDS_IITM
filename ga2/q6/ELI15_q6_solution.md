# Q6 Create a GitHub Action - ELI15 Step-by-Step

1. In your repo, create `.github/workflows/q6-action.yml`.
2. Add a workflow with:
   - trigger: `push` on `main`
   - trigger: `workflow_dispatch` (manual run)
3. Add one step name containing your email exactly:
   - `23f3001415@ds.study.iitm.ac.in`
4. Commit and push the workflow file.
5. Trigger the action:
   - either push another commit, or
   - run manually from Actions tab.
6. Verify in Actions tab that this run is the most recent.
7. Submit repository URL:
   - `https://github.com/23f3001415/TDS_IITM`
