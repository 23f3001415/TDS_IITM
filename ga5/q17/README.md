# Q17: LLM Image Generation JSON Body

## ELI15 Step-by-Step

1. API needs a JSON object (key-value pairs).
2. Put the image model name in `model`.
3. Put your scene description in `prompt`.
4. Put required output size in `size`.
5. Put number of images in `n`.
6. Make sure strings use double quotes (`"`), not single quotes.

## Final JSON Body

See `answer.json`.

